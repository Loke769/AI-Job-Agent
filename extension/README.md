# Extension —  Chrome toolbar
Load unpacked in chrome://extensions -> Developer mode -> Load unpacked -> select `extension/` folder.
It injects a toolbar on Greenhouse/Lever/Ashby job pages showing field count and one-click apply (demo).
